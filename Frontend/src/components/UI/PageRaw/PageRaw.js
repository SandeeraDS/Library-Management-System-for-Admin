import React from 'react';

const pageRaw = (props) => (
    <div className="row">
        <div className="col-lg-12">
            {props.children}
        </div>
    </div>
)

export default pageRaw;